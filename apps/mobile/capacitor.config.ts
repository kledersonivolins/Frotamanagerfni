import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'br.com.ferronorte.frotamanager',
  appName: 'FrotaManager',
  webDir: 'dist',
}

export default config
