import type { Loan } from './domain'
export function LoansHome({loans,onNew,onCalendar}:{loans:Loan[];onNew:()=>void;onCalendar:()=>void}){return <section><div className="title-row"><div><h2>Empréstimos</h2><p>Solicitações e veículos reservados</p></div><button className="primary" onClick={onNew}>Nova solicitação</button></div><button className="secondary wide" onClick={onCalendar}>Abrir calendário de disponibilidade</button><div className="cards">{loans.length?loans.map(x=><article className="item" key={x.id}><strong>{x.vehicleId}</strong><span>{x.destination}</span><em>{x.status.replaceAll('_',' ')}</em></article>):<div className="empty">Nenhuma solicitação armazenada neste aparelho.</div>}</div></section>}
