import {calculateProgress,type WorkOrder} from './domain'
export function WorkOrdersHome({orders,onOpen}:{orders:WorkOrder[];onOpen:(o:WorkOrder)=>void}){return <section><div className="title-row"><div><h2>Ordens de Serviço</h2><p>Execução e apontamentos offline</p></div></div><div className="cards">{orders.length?orders.map(o=><button className="item button-item" key={o.id} onClick={()=>onOpen(o)}><strong>OS {o.id}</strong><span>{o.description}</span><em>{calculateProgress(o.steps)}%</em></button>):<div className="empty">Nenhuma OS disponível no escopo deste usuário.</div>}</div></section>}
